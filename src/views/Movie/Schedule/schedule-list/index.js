import React from "react";
import ScheduleForm from "../../../schedule/form-schedule/MultyStepScheduleForm";
import MovieScheduler from "../components/MovieScheduler";

const Index = () => {
  return (
    <div>
      <MovieScheduler />;{/* <ScheduleForm mode="ADD" type='movie'/> */}
    </div>
  );
};

export default Index;
